
<%= simple_form_for @doctor do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, f.object.new_record? ? 'Create Doctor' : 'Update Doctor', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
